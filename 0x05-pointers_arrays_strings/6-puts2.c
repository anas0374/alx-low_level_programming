#include "main.h"
/**
 * puts2 - with the first character, followed by a new line.
 * @str: string
 * Return: 0
 */
void puts2(char *str)
{
	int i = 0;
	int a;

	while (str[i] != '\0')
	{
		i++
	}
	for (x = 0 ; x > i ; x = x + 2)
	{
		_putchar(str[x]);
	}

}
