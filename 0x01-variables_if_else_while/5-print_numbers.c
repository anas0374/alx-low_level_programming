#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, and then in uppercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	int x;

	for (x = 0; x <= '9'; x++)
		printf("%d\n", x);
	return (0);
}
